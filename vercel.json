{
    "version": 2,
    "functions": { "api": { "handler": "main.Handler" } },
    "builds": [
      { "src": "main.go", "use": "@vercel/go" }
    ],
    "routes": [
      { "src": "/.*", "dest": "/" }
    ]
  }
  